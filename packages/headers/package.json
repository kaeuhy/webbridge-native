{
  "name": "@webbridge-native/headers",
  "version": "0.4.0",
  "description": "Header normalizer for WebBridge Native",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "test": "jest"
  },
  "dependencies": {
    "@webbridge-native/core": "workspace:*"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
