{
  "name": "@webbridge-native/crypto",
  "version": "0.6.1",
  "description": "Web Crypto API polyfill (getRandomValues, randomUUID) for React Native",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "test": "jest"
  },
  "license": "MIT",
  "publishConfig": { "access": "public" }
}
